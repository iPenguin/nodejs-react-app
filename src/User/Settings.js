import React, { Component } from 'react';
import './Settings.css';

class UserSettings extends Component {
  render() {

    return (
      <div className="UserSettings-menu">
        <a href="">Settings</a>
      </div>
    );
  }
}

export default UserSettings;
